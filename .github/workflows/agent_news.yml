name: KidsNews Agent Pipeline

on:
  schedule:
    # Aiming for 5 AM PST / 6 AM PDT to arrive by 7 AM
    - cron: '0 13 * * *'   # 5 AM PST (winter)
    - cron: '0 12 * * *'   # 5 AM PDT (summer)
  workflow_dispatch:        # manual trigger from GitHub Actions tab

jobs:
  run_agent:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install anthropic firebase-admin sendgrid feedparser requests python-dotenv newsapi-python

      - name: Run agent pipeline
        working-directory: newsletter
        env:
          ANTHROPIC_API_KEY:       ${{ secrets.ANTHROPIC_API_KEY }}
          NEWS_API_KEY:            ${{ secrets.NEWS_API_KEY }}
          SENDGRID_API_KEY:        ${{ secrets.SENDGRID_API_KEY }}
          SENDER_EMAIL:            ${{ secrets.SENDER_EMAIL }}
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          GUARDIAN_API_KEY:        ${{ secrets.GUARDIAN_API_KEY }}
          GNEWS_API_KEY:           ${{ secrets.GNEWS_API_KEY }}
        run: python generate_news.py
